# Testdata

This is a testing zoo. Every `.in` file represents input to the parser that can
be processed. Every `.out` represents the text output from the parser if the
parsing is successful. If the parsing isn't successful the error is converted
into text and compared.

